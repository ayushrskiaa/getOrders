services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: orderhub
    volumes:
      - orderhub-postgres:/var/lib/postgresql/data

volumes:
  orderhub-postgres:
